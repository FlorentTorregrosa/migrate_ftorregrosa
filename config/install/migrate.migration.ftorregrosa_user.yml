id: ftorregrosa_user
label: ftorregrosa user
dependencies:
  enforced:
    module:
      - migrate_ftorregrosa
migration_groups:
  - ftorregrosa
source:
  plugin: ftorregrosa_user
destination:
  plugin: entity:user
process:
  uid: uid
  name: name
  pass: pass
  mail: mail
  status: status
  created: created
  changed: changed
  access: access
  login: login
  timezone: timezone
  langcode: language
  preferred_langcode: language
  preferred_admin_langcode: language
  init: init
